.content
  - if params[:day]
    %h2== Time entries for #{params[:day]}
  - else
    %h2 Time entries
    
  Total:
  = @sum

  - @entries.each do |project, entries|
    - if project
      %h3= project.name
    - else
      %h3 No project
    
    %table
      %tr
        %th Date
        %th User
        %th Story
        %th Minutes
        %th Hours
        %th
    
      - entries.each do |entry|
        %tr
          %td= entry.date
          %td= entry.user
          %td
            = commit_message(entry.story)
            = entry.note
          %td= entry.total_length
          %td= "%0.2f" % entry.hours
          %td
            = link_to "Edit", edit_time_entry_path(entry)
            = link_to "Remove", remove_time_entry_path(entry), :class => "ajax"
